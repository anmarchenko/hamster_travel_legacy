- trip ||= nil

- unless trip.blank?
  %div{class: 'thumbnail clearfix'}
    %div{class: 'clearfix'}
      %a{href: trip_path(trip)}
        %img{class: 'img-responsive', src: 'https://s3.amazonaws.com/altmer-cdn/images/no-image.png', width: '100%'}
    %div{class: 'caption'}
      %h4
        = link_to trip.name, trip_path(trip)
      = "#{t('.author')}:"
      - unless trip.author.blank?
        = link_to trip.author.full_name, user_path(trip.author)
      %br
