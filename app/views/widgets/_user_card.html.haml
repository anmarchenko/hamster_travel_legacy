.media
  = link_to user_path(user), class: 'pull-left' do
    %img.img-circle.media-object.small-profile-pic{src: user.image_url_or_default}
  .media-body
    .media-heading
      = link_to user.full_name, user_path(user)


