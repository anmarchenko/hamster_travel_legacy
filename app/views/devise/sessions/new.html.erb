<%= content_for :title, t('.title') %>

<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-8 col-md-6 col-sm-offset-2 col-md-offset-3">
      <h5><%= t('.welcome') %></h5>
      <hr/>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12 col-sm-8 col-md-6 col-sm-offset-2 col-md-offset-3">
      <%= link_to omniauth_authorize_path(resource_name, 'google_oauth2'), class:'btn btn-block btn-google btn-lg' do %>
        <%= t('devise.google') %>
      <% end %>
    </div>
  </div>
  <div class="row login-variant">
    <div class="col-xs-12 col-sm-8 col-md-6 col-sm-offset-2 col-md-offset-3">
      <%= t('common.login_variant') %>
    </div>
  </div>
  <div class="row login-form">
    <div class="col-xs-12 col-sm-8 col-md-6 col-sm-offset-2 col-md-offset-3">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name), role: 'formr') do |f| %>
          <fieldset>
            <div class="form-group">
              <%= f.email_field :email, autofocus: true, class: 'form-control input-lg', placeholder: t('.email') %>
            </div>
            <div class="form-group">
              <%= f.password_field :password, autocomplete: 'off', class: 'form-control input-lg', placeholder: t('.password') %>
            </div>
            <div class="form-group login-link">
              <a href="<%= new_password_path(resource_name) %>">
                <%= t('common.forgot_password') %>
              </a>
            </div>
            <div class="form-group">
              <span class="button-checkbox">
                <%= f.check_box :remember_me, checked: 'checked' %>
                <%= f.label :remember_me, t('.remember') %>
              </span>
            </div>
            <div class="form-group">
              <%= f.submit t('.sign_in'), class: 'btn btn-lg btn-success btn-block' %>
            </div>
          </fieldset>
      <% end %>
    </div>
  </div>
</div>
